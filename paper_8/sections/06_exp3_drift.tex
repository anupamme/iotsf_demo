% ============================================================
\section{Experiment 3: Can Measured Drift Replace the Frozen Control?}
\label{sec:exp3}
% ============================================================

This is the section the paper's title is about.  CKA tells us that the
representation changed; Experiment 2's intervention tells us whether the change
was valuable.  Neither CKA nor weight drift is manipulated by that design
(\S\ref{sec:method}), so both enter here only as candidate \emph{predictors} of
$\denc$---and \textbf{across the cells we evaluate, the former does not reliably
predict the latter.}

\label{sec:forecasting:nonmoirai}
\textbf{Drift and outcome come apart inside a single backbone}---within our
Moirai matrix, the strongest form of the finding.  Beyond the sweep of
\S\ref{sec:forecasting:replication}: within Moirai-Base the two \emph{least}
drifted cells are the only two that degrade (ETTh1 at CKA $0.755$ and $0.669$,
$+22.8\%$ and $+39.5\%$) while its two most drifted improve the most within the
arm (ETTh2 at CKA $0.460$ and $0.396$, $-31.4\%$ and $-36.4\%$; the whole
eight-cell ordering is Appendix Table~\ref{tab:dissociation}); and inside
Moirai-Large's LoRA arm the learning rate, not the rank, decides the sign at
near-identical CKA ($0.973$ against $0.989$; Appendix~\ref{app:lora_rank}).

\textbf{Near-identical drift, opposite consequences.}  Against the fitted baseline
none of the ten non-Moirai cells gate-passes, so what the two extra backbones supply
is architectural diversity and the dissociation itself, \textbf{not a controlled
replication}: they run at $h{=}24$ against Moirai's $h{\in}\{96,192\}$ and on
different normalisations, and we read them qualitatively and never pool them.  Four
of the five Chronos-T5-Small cells restructure to a degree that would, on the usual
reading, be called catastrophic---CKA ${\in}[0.092, 0.169]$, at or below the
$0.215{\pm}0.054$ an \emph{untrained} encoder of this architecture scores
(Appendix~\ref{app:cka_calibration})---yet outcomes across the five span
\textbf{$-41.4\%$ on ETTm2 to $+20.6\%$ on ETTh1}, a 62-point spread in which ETTh1
is the \emph{least} drifted and the only one that degrades; the four TimesFM-2.5
cells sit at milder drift and all improve
(Appendix Table~\ref{tab:crossbackbone}).  Pooled across all 31 cells CKA does correlate with the intervention
($\rho{=}{+}0.567$, clustered CI $[{+}0.239,{+}0.828]$), but that pooled figure is a
backbone artefact by our own rule: within Moirai, the only arm with enough
cells to ask, $\rho{=}{+}0.168$ with clustered CI $[-0.469,{+}0.717]$, which admits a
moderate positive relationship but \textbf{does not provide a reliable ordering}
of our cells and yields no actionable threshold.  Nor does the
second diagnostic \S\ref{sec:method} defines rescue it: pooled $\ell_2$ weight
drift gives $\rho{=}{+}0.118$ with a CI including zero, and nor do five further
geometric measures
(Appendices~\ref{app:crosscell},~\ref{app:driftmetrics}).  The same dissociation
holds \emph{inside} one encoder: unfreezing the top 3 of 6 layers and unfreezing
all 6 are indistinguishable on the task at CKA $0.716$ against $0.465$
(Appendix~\ref{app:layerunfreeze}).  And where a cell does degrade,
freezing does not save it: on Chronos/ETTh1 the frozen encoder is worse still
($+59.8\%$ against $+20.6\%$), so encoder adaptation is not the locus of the damage.

\textbf{CKA adds no predictive information beyond backbone identity.}  A wide
interval is a failure to establish an ordering, not evidence against one, so with
the ladder and deciding statistic fixed in advance
(\texttt{results/preregister\_loco.json}) we asked out of sample whether measuring
CKA helps a reader who already knows the backbone, the horizon and $n$.  It does
not: leave-one-cluster-out $R^2$ over the 15 clusters rises from $+0.000$ to
$+0.137$ on backbone identity and $+0.201$ with horizon and $\log n$, then
\emph{falls} to $+0.176$ once CKA is added ($\Delta R^2{=}{-}0.025$; no help in
$69\%$ of cluster-bootstrap draws), and the backbone-specific-slope variant is
worst of all at $+0.019$.  So the predictable part of $\denc$ is predictable from
protocol identity---though the slope itself, $+43$~pp per unit CKA with a cluster
interval of $[-31, +107]$, makes this a null in \emph{prediction} rather than
evidence of no relationship (Appendix~\ref{app:loco}).

% Two paragraphs were cut from here: the graded-screen robustness check (rho on the 20 gate-clearing
% cells against the 11 others, and the b_3 interaction) and the ILI case study's numbers. Both are
% reported in full in the appendices pointed at below, which is where they were already reported -- the
% body was restating them. Cut so that this section spends its lines on the dissociation itself, which
% is the paper's one sentence, rather than on two objections it also answers.
\textbf{Not an artefact of cells with nothing to lose.}  Drift might fail to order
$\denc$ only because 11 of the 31 cells beat no rung of the ladder; grading the
screen rather than dichotomising it says otherwise---the relation is no stronger on
the cells with a measured advantage than on the cells without, and the estimated
interaction has the wrong sign for the objection and an interval far too wide to
settle it (Appendix~\ref{app:valueaxis}).

\textbf{ILI: a small-data external case study.}
\label{sec:analysis:ili}
Moirai-Small on ILI (weekly influenza, $h{=}24$, 10 seeds) restructures heavily
and improves, yet \textbf{gate-fails decisively} at
$\Vb{\text{ridge}}{=}{-}1.625$; at ${\approx}450$ windows it is a case study
rather than a pillar, and its pre-registered CKA prediction was
\textbf{falsified} (Appendix~\ref{app:ili_gate}).  An IoT anomaly-detection
negative control behaves the same way (Appendix~\ref{app:cnn_detail}).

