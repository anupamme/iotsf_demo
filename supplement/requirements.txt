# Reproducibility bundle requirements
# Tested with Python 3.12 (required for uni2ts compatibility)

numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0
scikit-learn>=1.3.0
torch>=2.0.0
einops>=0.6.0
transformers>=4.30.0
pyyaml>=6.0
tqdm>=4.60.0
loguru>=0.7.0

# Moirai (uni2ts) - requires Python 3.12
# Install via: pip install "uni2ts @ git+https://github.com/SalesforceAIResearch/uni2ts.git"
# After install, apply: patches/uni2ts_packed_std_scaler.patch
